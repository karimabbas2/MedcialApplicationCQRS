import React from 'react';
import MyNavbar from '../Navbar';
import Cards from '../Cards';

const Header = () => {
    return (
        <>
            <MyNavbar />
            <Cards />
        </>
    );
};

export default Header;